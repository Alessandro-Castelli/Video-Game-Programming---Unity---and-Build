/*
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VignetteDelete : MonoBehaviour
{
    public float timer = 5f;
    void Start()
    {
        // Avvia il timer per la distruzione dell'oggetto dopo 5 secondi
        Invoke("DestroyVignette", timer);
    }

    void DestroyVignette()
    {
        // Distruggi l'oggetto della vignetta
        Destroy(gameObject);
    }
}
*/